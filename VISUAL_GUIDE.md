# 🎯 Visual Integration Guide

## 📊 System Architecture Diagram

```
╔════════════════════════════════════════════════════════════════╗
║                    EXPENSE TRACKER APP                        ║
╚════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│                    USER'S BROWSER                            │
│              (Windows, Mac, Linux)                           │
└──────────────────────────────────────────────────────────────┘
                            │
                    Opens http://localhost:3000
                            │
        ┌───────────────────▼────────────────────┐
        │   FRONTEND - React + TypeScript        │
        │   Port: 3000                          │
        │   ┌──────────────────────────────┐   │
        │   │ Pages                        │   │
        │   │ ├─ Auth (Login/Signup)      │   │
        │   │ ├─ Dashboard                 │   │
        │   │ ├─ Expenses                  │   │
        │   │ ├─ Budget                    │   │
        │   │ ├─ Insights                  │   │
        │   │ └─ Settings                  │   │
        │   │                              │   │
        │   │ Components: UI widgets      │   │
        │   │ Services: API calls         │   │
        │   │ Hooks: State management     │   │
        │   │ Storage: localStorage       │   │
        │   └──────────────────────────────┘   │
        └───────────────────┬────────────────────┘
                            │
                    Sends HTTP Requests
                    Content-Type: JSON
                    Authorization: Bearer JWT
                            │
        ┌───────────────────▼────────────────────┐
        │   BACKEND - .NET 8 API                │
        │   Port: 5196                         │
        │   ┌──────────────────────────────┐   │
        │   │ Controllers                  │   │
        │   │ ├─ AuthController           │   │
        │   │ ├─ ExpensesController       │   │
        │   │ └─ DashboardController      │   │
        │   │                              │   │
        │   │ Services: Business logic    │   │
        │   │ DTOs: Data transfer         │   │
        │   │ Auth: JWT validation        │   │
        │   │ Exception: Error handling   │   │
        │   └──────────────────────────────┘   │
        └───────────────────┬────────────────────┘
                            │
                    Performs Database Operations
                    Connection: MongoDB Protocol
                            │
        ┌───────────────────▼────────────────────┐
        │   DATABASE - MongoDB                  │
        │   Port: 27017                        │
        │   Database: ExpenseTrackerDB         │
        │   ┌──────────────────────────────┐   │
        │   │ Collections:                 │   │
        │   │ ├─ users                     │   │
        │   │ ├─ expenses                  │   │
        │   │ ├─ budgets                   │   │
        │   │ ├─ categories                │   │
        │   │ └─ recurringExpenses         │   │
        │   │                              │   │
        │   │ Storage: BSON documents      │   │
        │   │ Indexing: Optimized queries  │   │
        │   └──────────────────────────────┘   │
        └───────────────────────────────────────┘
```

## 🔄 Authentication Flow

```
STEP 1: REQUEST OTP
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ User enters email/phone → Click "Send OTP"              │
│ POST /Auth/send-otp { email, phone }                    │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend                                                  │
│ AuthController receives request                         │
│ AuthService.SendOtpAsync()                             │
│ Generate OTP → Store in MongoDB                        │
│ Return { success: true }                               │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ Show "OTP sent successfully"                           │
│ Display OTP verification form                          │
└──────────────────────────────────────────────────────────┘

STEP 2: VERIFY OTP
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ User enters OTP → Click "Verify"                        │
│ POST /Auth/verify-otp { email, phone, otp }           │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend                                                  │
│ AuthController.VerifyOtp()                             │
│ Check OTP against MongoDB                              │
│ If valid: { verified: true }                           │
│ If invalid: { verified: false, error }                │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ If verified: Show signup/login form                    │
│ If not: Show error message                             │
└──────────────────────────────────────────────────────────┘

STEP 3: SIGNUP or LOGIN
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ User fills form → Click "Sign Up" or "Login"           │
│ POST /Auth/signup or /Auth/login + OTP query param    │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend                                                  │
│ AuthController.Signup() or Login()                     │
│ Create user OR Find existing user                      │
│ Generate JWT token                                     │
│ Return { user, token }                                 │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend                                                 │
│ Store token: localStorage.setItem('authToken', token) │
│ Redirect to Dashboard                                  │
│ All subsequent requests include: Authorization header │
└──────────────────────────────────────────────────────────┘
```

## 💾 Expense Management Flow

```
ADD EXPENSE
┌──────────────────────────────────────────────────────────┐
│ Frontend - User Interface                               │
│ Fill form: Amount, Category, Description, Date         │
│ Click "Add Expense" button                              │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend - Service Layer                                │
│ expenseApi.createExpense({                              │
│   amount: 50.00,                                        │
│   category: "Food",                                     │
│   description: "Lunch",                                │
│   date: "2024-02-02"                                   │
│ })                                                      │
│                                                         │
│ POST /Expenses                                          │
│ Headers: Authorization: Bearer {JWT_TOKEN}             │
│ Content-Type: application/json                         │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend - API Layer                                     │
│ ExpensesController.CreateExpense()                     │
│ ├─ Validate JWT token                                  │
│ ├─ Extract userId from token                           │
│ └─ Validate request data                               │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend - Service Layer                                 │
│ ExpenseService.CreateExpenseAsync()                    │
│ ├─ Create Expense entity                               │
│ ├─ Set userId, timestamps                              │
│ └─ Pass to data layer                                  │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Backend - Data Access Layer                             │
│ MongoDbContext.ExpensesCollection                      │
│ ├─ InsertOneAsync(expense)                             │
│ └─ MongoDB generates ObjectId                          │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ MongoDB Database                                        │
│ db.expenses.insertOne({                                 │
│   "_id": ObjectId(...),                                │
│   "userId": "user123",                                 │
│   "amount": 50.00,                                     │
│   "category": "Food",                                  │
│   "description": "Lunch",                              │
│   "date": ISODate(...),                                │
│   "createdAt": ISODate(...),                           │
│   "updatedAt": ISODate(...)                            │
│ })                                                      │
│                                                         │
│ Insert successful ✓                                    │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼ Response travels back through layers
┌──────────────────────────────────────────────────────────┐
│ Backend returns HTTP 200 OK                             │
│ Body: ApiResponse<Expense>                             │
│ {                                                       │
│   "success": true,                                      │
│   "data": {                                             │
│     "id": "65c...",                                     │
│     "amount": 50.00,                                    │
│     "category": "Food",                                │
│     "description": "Lunch",                            │
│     "date": "2024-02-02",                              │
│     "createdAt": "2024-02-02T10:30:00Z"               │
│   }                                                     │
│ }                                                       │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend - API Service                                  │
│ Parse response JSON                                    │
│ Extract expense data                                   │
│ Return to React component                              │
└──────────────────────┬──────────────────────────────────┘
                       │
                       ▼
┌──────────────────────────────────────────────────────────┐
│ Frontend - React Component                              │
│ ├─ Show success notification                            │
│ ├─ Add expense to list                                  │
│ ├─ Update dashboard totals                              │
│ ├─ Clear form                                           │
│ └─ Re-render UI                                         │
└──────────────────────────────────────────────────────────┘

User sees the new expense in the list! ✓
```

## 🔐 JWT Token Flow

```
LOGIN RESPONSE FROM BACKEND:
┌─────────────────────────────────────┐
│ Backend generates:                  │
│                                     │
│ JWT Token Structure:                │
│ Header.Payload.Signature            │
│                                     │
│ HEADER: { typ: JWT, alg: HS256 }   │
│ PAYLOAD: {                          │
│   sub: "userId123",                 │
│   email: "user@example.com",        │
│   iss: "ExpensesBackend",          │
│   aud: "ExpensesBackend",          │
│   exp: 1706...,                     │
│   iat: 1705...                      │
│ }                                   │
│ SIGNATURE: HMACSHA256(...)          │
│                                     │
│ Returns JWT token to frontend       │
└─────────────────────────────────────┘
                │
                ▼
FRONTEND STORES TOKEN:
┌─────────────────────────────────────┐
│ localStorage.setItem(                │
│   'authToken',                      │
│   'eyJhbGc...'                      │
│ )                                   │
└─────────────────────────────────────┘
                │
                ▼
EVERY API REQUEST INCLUDES TOKEN:
┌─────────────────────────────────────┐
│ GET /Expenses                        │
│ Headers: {                           │
│   'Authorization': 'Bearer eyJhbGc...'
│ }                                   │
└─────────────────────────────────────┘
                │
                ▼
BACKEND VALIDATES TOKEN:
┌─────────────────────────────────────┐
│ Verify signature                    │
│ Check expiration                    │
│ Extract userId from claims          │
│ Proceed with request                │
│ Or return 401 Unauthorized          │
└─────────────────────────────────────┘
```

## 📈 Data Flow Summary

```
USER ACTION
    ↓
[Frontend Component]
    ↓
[React State/Props]
    ↓
[API Service Call]
    ↓ HTTP Request (JSON)
[Backend API Endpoint]
    ↓
[JWT Validation]
    ↓
[Business Logic Service]
    ↓
[Data Validation]
    ↓
[MongoDB Query]
    ↓
[Database Operation]
    ↓ Response
[Service Layer]
    ↓
[API Response (JSON)]
    ↓ HTTP Response
[Frontend Service]
    ↓
[React State Update]
    ↓
[Component Re-render]
    ↓
USER SEES UPDATED UI ✓
```

## 🚀 Startup Sequence

```
START SCRIPT EXECUTION:

1. Check MongoDB
   │
   ├─ If running ✓ → Continue
   └─ If not running ⚠ → Warn user
   
2. Start Backend (New Window)
   │
   ├─ Load appsettings.json
   ├─ Connect to MongoDB
   ├─ Register services
   ├─ Setup CORS
   ├─ Setup JWT auth
   ├─ Setup Swagger
   └─ Listen on :5196 ✓

3. Start Frontend (New Window)
   │
   ├─ Load .env
   ├─ Install/Load npm packages
   ├─ Configure Vite
   ├─ Setup proxy to :5196
   └─ Serve on :3000 ✓

4. Open Browser
   │
   └─ Navigate to localhost:3000

Ready to use! ✓
```

## 📊 Component Communication Map

```
                         API CALLS
        ┌────────────────────────────────────┐
        │                                    │
   [Components]                        [Services]
        │                                    │
   ├─ Auth Pages            ────→    auth.api.ts
   ├─ Expense Pages         ────→    expense.api.ts
   ├─ Dashboard Pages       ────→    dashboard.api.ts
   ├─ Budget Pages          ────→    budget.api.ts
   └─ Settings Pages        ────→    settings.api.ts
        │                                    │
   [Hooks]                              [api.service.ts]
        │                                    │
   ├─ useAuth              ────→     Fetch API
   ├─ useApi               ────→    JWT Headers
   ├─ useTheme             ────→     Error Handling
   └─ useMediaQuery        ────→    Data Parsing
        │                                    │
        │         ────────────────────────────
        │         │
        ▼         ▼
    [State]    [Backend API]
        │         │
        │    [Controllers]
        │         │
        │    [Services]
        │         │
        │    [MongoDB]
```

---

This visual guide helps understand how all components work together! 🎯
